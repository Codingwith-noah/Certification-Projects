.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border: 0;
}

^^ This makes the text within the span class of the HTML invisible,
this is useful so that viewers with no screen-reader are not able to
view this feature